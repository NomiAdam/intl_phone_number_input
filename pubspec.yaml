name: intl_phone_number_input
description: A simple and customizable flutter package for inputting phone number in intl / international format uses Google's libphonenumber.
version: 0.7.2
homepage: https://github.com/natintosh/intl-phone-number-input

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  libphonenumber_plugin: 0.3.3
  equatable: ^2.0.7
  collection: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  libphonenumber_web:
    git:
      url: https://github.com/NomiAdam/plugin_libphonenumber.git
      ref: migrate_to_next_gen_js_interop
      path: libphonenumber_web

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  assets:
    - assets/flags/
